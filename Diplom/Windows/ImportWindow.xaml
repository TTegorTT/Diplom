<Window x:Class="Diplom.Windows.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт сотрудников из Excel" 
        Height="450" Width="650"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="LightGray">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Импорт данных из Excel" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Выбор файла -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Выбрать файл Excel" Width="140" Height="30" Click="ChooseFile_Click"/>
            <TextBlock x:Name="FilePathText" Text="Файл не выбран" VerticalAlignment="Center" Margin="10,0,0,0"
                       Foreground="Gray" FontSize="13"/>
        </StackPanel>

        <!-- Кнопка импорта -->
        <Button Grid.Row="2" Content="▶ Выполнить импорт" Width="160" Height="35" 
                Click="Import_Click" IsEnabled="False" x:Name="ImportButton" Margin="0,0,0,10"/>

        <!-- Инструкция (ожидаемые столбцы) -->
        <TextBlock Grid.Row="3" FontSize="12" Foreground="#555" Margin="0,0,0,10"
                   Text="Ожидаемые столбцы (по порядку): Фамилия, Имя, Отчество, Отдел, Должность, Тип занятости"/>

        <!-- Лог -->
        <Border Grid.Row="4" Background="White" CornerRadius="5" BorderBrush="Gray" BorderThickness="1"
                Padding="8">
            <TextBox x:Name="LogTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                     TextWrapping="Wrap" Background="Transparent" BorderThickness="0"/>
        </Border>

        <!-- Закрыть -->
        <Button Grid.Row="5" Content="Закрыть" Width="80" Height="30" 
                HorizontalAlignment="Right" Margin="0,10,0,0" Click="Close_Click"/>
    </Grid>
</Window>