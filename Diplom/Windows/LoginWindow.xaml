<Window x:Class="Diplom.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diplom.Windows"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Логин"/>
                <Border Padding="3" CornerRadius="3" 
                    BorderBrush="LightGray" BorderThickness="1">
                    <TextBox BorderThickness="0" x:Name="Login"/>
                </Border>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Пароль"/>
                <Border Padding="3" CornerRadius="3" 
                    BorderBrush="LightGray" BorderThickness="1">
                    <PasswordBox BorderThickness="0" x:Name="Password"/>
                </Border>
            </StackPanel>
            <Border Padding="3" CornerRadius="3" 
                BorderBrush="LightGray" BorderThickness="1"
                Margin="0, 15, 0, 0">
                <Button Content="Вход" Background="Transparent"
                    BorderThickness="0" Click="LoginButton_Click"/>
            </Border>
            <Border Padding="3" CornerRadius="3" 
                BorderBrush="LightGray" BorderThickness="1"
                Margin="0, 15, 0, 0">
                <Button Content="Отмена" Background="Transparent"
                    BorderThickness="0"/>
            </Border>
        </StackPanel>
    </Grid>
</Window>
